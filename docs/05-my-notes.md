# My Notes

I have used integer, representing the lowest denomination (e.g., cents, pence). This approach avoids the potential precision issues inherent in using floating-point numbers. 

I have conflicts with Tailwind and bootstrap, which I used. These need resolving, but only causing minor cosmetic issues.

I would like to remove the CSS and JavaScript out of app.blade.php

I didn’t get to the automated testing, time against me.

There is validation in the SalesController.

There is seeding for Products.
